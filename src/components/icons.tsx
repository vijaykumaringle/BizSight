import { Calendar, DollarSign, File, Package, Percent, User, FileText, ShoppingCart } from "lucide-react";
import { LayoutDashboard } from "lucide-react";

export const Icons = {
    calendar: Calendar,
    dashboard: LayoutDashboard,
    income: DollarSign,
    expense: File,
    taxInsights: Percent,
    user: User,
    invoice: FileText,
    inventory: Package,
    order: ShoppingCart,
};